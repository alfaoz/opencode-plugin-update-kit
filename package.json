{
  "name": "opencode-plugin-update-kit",
  "version": "0.1.0",
  "description": "Auto-update kit for opencode plugins — detects new versions, installs them, and notifies the user. Handles concurrency when multiple plugins use it.",
  "type": "module",
  "main": "index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "typecheck": "bun build index.ts --target=bun --outdir /tmp/update-kit-check"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "auto-update"
  ],
  "license": "MIT"
}
